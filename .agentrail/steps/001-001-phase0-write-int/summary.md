Created src/runtime.spc with _p24p_write_int Phase 0 stub. The routine uses enter/leave/ret for proper frame management, loada 0 to get the argument, checks sign (lt 0 → prints '-' and negates), extracts digits via div/mod 10 loop pushing ASCII chars onto eval stack with zero sentinel, then prints digits most-significant-first via sys 1. Added tests/test_write_int.spc exercising 42, 0, -123, 7, 999999. Cannot end-to-end test yet because pasm parser in pv24a is incomplete (lexer only).