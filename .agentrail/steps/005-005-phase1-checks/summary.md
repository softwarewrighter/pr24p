Added _p24p_bounds_check and _p24p_nil_check to runtime.spc with .export declarations. bounds_check takes 3 args (index, low, high) and prints BOUNDS+halts on violation. nil_check takes ptr and prints NIL+halts if zero, else returns ptr. Created src/checks.pas (aspirational Pascal source) and tests/test_checks.spc (verified on VM: OK/42/NIL output). Fixed argument ordering discovery: loada 0 = last-pushed arg (TOS at call time).